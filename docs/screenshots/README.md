# Screenshots

Placeholder SVGs live here until real captures are taken in a desktop JDownloader.

Needed for v1.0.0:

- `download-list.svg` → replace with a PNG of the **download list** in JD Plain Dark.
- `settings.svg` → replace with a PNG of the **Advanced Settings** table in JD Plain Dark.

When replacing with PNGs, update the image paths in the root `README.md` (section 2).
